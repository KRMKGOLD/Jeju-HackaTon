package com.example.hackatonproject.data

data class Repo(val latitude : String, val longitude : String)
//data class JsonObject(val row : Int, val column : Int)
//data class Repo(val latitude : Int, val longitude : Int)
data class Repo2(val latitude : String, val longitude : String)